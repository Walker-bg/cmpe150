
def draw_triangles():
    x = input()  # Assume that input is taken here, so use x

    # DO_NOT_EDIT_ANYTHING_ABOVE_THIS_LINE
    x = int(x)
    for num in range(x, 0, -1):
            print('*' * num)
    # DO_NOT_EDIT_ANYTHING_BELOW_THIS_LINE


draw_triangles()